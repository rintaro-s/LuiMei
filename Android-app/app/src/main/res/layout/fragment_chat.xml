<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_color">

    <!-- Session info -->
    <TextView
        android:id="@+id/tvSessionInfo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="セッション: 不明"
        android:textSize="12sp"
        android:textColor="@color/text_secondary"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp" />

    <!-- Chat Messages RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewChat"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="16dp"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
        app:layout_constraintBottom_toTopOf="@+id/input_container"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Input Container -->
    <androidx.cardview.widget.CardView
        android:id="@+id/input_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="8dp"
        app:cardBackgroundColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="16dp"
            android:gravity="center_vertical">

            <!-- Text Input -->
            <EditText
                android:id="@+id/editTextMessage"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="メッセージを入力..."
                android:background="@null"
                android:maxLines="3"
                android:textSize="16sp"
                android:layout_marginEnd="12dp" />

            <!-- Send Button (expected id by binding) -->
            <Button
                android:id="@+id/buttonSend"
                style="?attr/borderlessButtonStyle"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:backgroundTint="@color/primary_color"
                android:textColor="@color/white"
                android:text="送信" />

            <!-- Hidden/aux voice button used by binding (appears as small button) -->
            <Button
                android:id="@+id/buttonVoice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                android:text="🎤" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

    <!-- Central Voice Button (Push-to-Talk) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/btn_voice_main"
        android:layout_width="80dp"
        android:layout_height="80dp"
        app:cardCornerRadius="40dp"
        app:cardElevation="12dp"
        app:cardBackgroundColor="@color/primary_color"
        app:layout_constraintBottom_toTopOf="@+id/input_container"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginBottom="24dp">

        <ImageView
            android:id="@+id/iv_voice_main"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_mic"
            android:tint="@color/white" />

    </androidx.cardview.widget.CardView>

    <!-- Voice Status Text -->
    <TextView
        android:id="@+id/tv_voice_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="長押しで音声入力"
        android:textSize="14sp"
        android:textColor="@color/text_primary"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@+id/btn_voice_main"
        app:layout_constraintEnd_toEndOf="@+id/btn_voice_main"
        app:layout_constraintStart_toStartOf="@+id/btn_voice_main"
        android:layout_marginBottom="8dp" />

    <!-- TTS Toggle Button -->
    <androidx.cardview.widget.CardView
        android:id="@+id/btn_tts"
        android:layout_width="48dp"
        android:layout_height="48dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="4dp"
        app:cardBackgroundColor="@color/secondary_color"
        app:layout_constraintBottom_toBottomOf="@+id/btn_voice_main"
        app:layout_constraintEnd_toStartOf="@+id/btn_voice_main"
        app:layout_constraintTop_toTopOf="@+id/btn_voice_main"
        android:layout_marginEnd="24dp">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_volume_up"
            android:tint="@color/white" />

    </androidx.cardview.widget.CardView>

    <!-- Voice Character Selection Button -->
    <androidx.cardview.widget.CardView
        android:id="@+id/btn_voice_settings"
        android:layout_width="48dp"
        android:layout_height="48dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="4dp"
        app:cardBackgroundColor="@color/secondary_color"
        app:layout_constraintBottom_toBottomOf="@+id/btn_voice_main"
        app:layout_constraintStart_toEndOf="@+id/btn_voice_main"
        app:layout_constraintTop_toTopOf="@+id/btn_voice_main"
        android:layout_marginStart="24dp">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_settings"
            android:tint="@color/white" />

    </androidx.cardview.widget.CardView>

    <!-- New session and history buttons -->
    <LinearLayout
        android:id="@+id/session_controls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_margin="16dp">

        <Button
            android:id="@+id/btnNewSession"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="新規" />

        <Button
            android:id="@+id/btnHistory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="履歴"
            android:layout_marginStart="8dp" />

    </LinearLayout>

    <!-- Recording Animation Overlay -->
    <androidx.cardview.widget.CardView
        android:id="@+id/voice_recording_overlay"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:visibility="gone"
        app:cardCornerRadius="60dp"
        app:cardElevation="16dp"
        app:cardBackgroundColor="@color/error_color"
        app:layout_constraintBottom_toBottomOf="@+id/btn_voice_main"
        app:layout_constraintEnd_toEndOf="@+id/btn_voice_main"
        app:layout_constraintStart_toStartOf="@+id/btn_voice_main"
        app:layout_constraintTop_toTopOf="@+id/btn_voice_main">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center">

            <ImageView
                android:id="@+id/iv_voice_recording"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_mic"
                android:tint="@color/white" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="録音中..."
                android:textColor="@color/white"
                android:textSize="12sp"
                android:textStyle="bold" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

    <!-- Recording indicator (hidden by default) -->
    <LinearLayout
        android:id="@+id/recordingIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@+id/btn_voice_main"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="24dp"
        android:layout_marginBottom="12dp">

        <View android:id="@+id/recordingDot1" android:layout_width="8dp" android:layout_height="8dp" android:background="@drawable/circle_red" android:alpha="0.3" />
        <View android:id="@+id/recordingDot2" android:layout_width="8dp" android:layout_height="8dp" android:background="@drawable/circle_red" android:layout_marginStart="6dp" android:alpha="0.3" />
        <View android:id="@+id/recordingDot3" android:layout_width="8dp" android:layout_height="8dp" android:background="@drawable/circle_red" android:layout_marginStart="6dp" android:alpha="0.3" />

    </LinearLayout>

    <!-- Loading overlay used during API calls -->
    <FrameLayout
        android:id="@+id/loadingOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        android:background="#80000000"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <TextView
            android:id="@+id/loadingText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="読み込み中..."
            android:textColor="@color/white"
            android:textSize="16sp"
            android:layout_gravity="center" />

    </FrameLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
